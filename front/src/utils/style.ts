import { createTheme } from "@mui/material";

export const primaryColor = createTheme({
    palette: {
        primary: {
            main: '#d47676'
        }
    }
})

export const secondaryColor = createTheme({
    palette: {
        primary: {
            main: '#242424'
        }
    }
})

